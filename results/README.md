# Results Directory

This directory is for generated pipeline outputs. Files produced by the scripts are ignored by git by default so repeated searches, extraction runs, and translation worksheets do not create noisy commits.

If a generated result becomes a curated fixture or example, move it to `examples/` or adjust `.gitignore` intentionally.
